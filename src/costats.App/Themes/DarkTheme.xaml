<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Text -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F0F0F0" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#C7C7C7" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="#9E9E9E" />
    <SolidColorBrush x:Key="TextFaintBrush" Color="#757575" />

    <!-- Structure -->
    <SolidColorBrush x:Key="DividerBrush" Color="#3A3A3A" />
    <SolidColorBrush x:Key="TrackBrush" Color="#333333" />
    <SolidColorBrush x:Key="AccentBrush" Color="#818CF8" />

    <!-- Surfaces: #202020 base, #2b2b2b secondary (cards/inputs/buttons) -->
    <SolidColorBrush x:Key="WidgetBackgroundBrush" Color="#202020" />
    <SolidColorBrush x:Key="SettingsBackgroundBrush" Color="#202020" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#2B2B2B" />

    <!-- Overlays (hover/pressed states on buttons) -->
    <SolidColorBrush x:Key="OverlayHoverBrush" Color="#14FFFFFF" />
    <SolidColorBrush x:Key="OverlayPressedBrush" Color="#20FFFFFF" />
    <SolidColorBrush x:Key="ButtonBaseBrush" Color="#2B2B2B" />

    <!-- Cost figures (WCAG AA on dark background) -->
    <SolidColorBrush x:Key="CostTodayAccent" Color="#34D399" />
    <SolidColorBrush x:Key="CostMonthAccent" Color="#A5B4FC" />

    <!-- Plan chip text (WCAG AA on tinted chip background) -->
    <SolidColorBrush x:Key="PlanChipCodexText" Color="#34D399" />
    <SolidColorBrush x:Key="PlanChipClaudeText" Color="#FDBA74" />
    <SolidColorBrush x:Key="PlanChipCopilotText" Color="#C4B5FD" />

    <!-- Multicc panel hero percentage text -->
    <SolidColorBrush x:Key="SessionTextBrush" Color="#FBBF24" />
    <SolidColorBrush x:Key="WeeklyTextBrush" Color="#C4B5FD" />

    <!-- Tray icon context menu (implicit styles - applies app-wide) -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.4" Color="Black" />
                        </Border.Effect>
                        <StackPanel IsItemsHost="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="border" Background="Transparent" CornerRadius="5" Margin="2,1">
                        <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource OverlayHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextFaintBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource DividerBrush}" />
        <Setter Property="Margin" Value="6,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" Height="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
